import {css} from '@emotion/react'

const container = css `
    min-height: 100vh;
    padding: 0 0.5rem;
    display: grid;
    place-items: center;
  `
  const title = css `
    margin: 0;
    line-height: 1.15;
    font-size: 4rem;
    text-align: center;
  `

  export {container, title}